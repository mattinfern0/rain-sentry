<header>
    <nav>
        <ul>
            <% for (let i = 0; i < navigation.length; i++) { %>
                <li>
                    <a href='<%= navigation[i].link %>'>
                        <%= navigation[i].name %>
                    </a>
                </li>
            <% } %>
        </ul>
    </nav>
</header>