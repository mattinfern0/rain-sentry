<html>
<head>
    <% include ./partials/head %>
</head>
<body>
    <header>
        <% include ./partials/header %>
    </header>
    <% if (rainyDays.length > 0) { %>
        <ul>
            <% for (let i = 0; i < rainyDays.length; i++) { %>
                <li><%= `${rainyDays[i].description} @ ${rainyDays[i].parsedTime}` %></li>
            <% } %>
        </ul>
    <% } else { %>
        <p>All dry!</p>
    <% } %>
</body>
</html>